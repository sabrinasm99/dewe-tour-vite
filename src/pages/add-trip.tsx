import Layout from "../modules/shared/layout/Layout";
import AddTrip from "../modules/trips/features/add-trip";

export default function AddTripPage() {
  return (
    <Layout>
      <AddTrip />
    </Layout>
  );
}
